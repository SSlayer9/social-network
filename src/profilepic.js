import React from "react";

export default function ProfilePic(props) {
    return (
        <div>
            <h1>Profile Pic running</h1>
            <h2>Welcome, {props.first}</h2>
            <img />
        </div>
    );
}
